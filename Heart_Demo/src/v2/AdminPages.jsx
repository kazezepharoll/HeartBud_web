import {useEffect,useState} from 'react';import axios from 'axios';import AppShell,{SectionCard,StatusBadge} from './AppShell';const API=import.meta.env.VITE_API_BASE_URL||'http://localhost:3000';
export function AdminUsers(){const [users,setUsers]=useState([]);const [loading,setLoading]=useState(true);useEffect(()=>{axios.get(`${API}/admin/users`).then(r=>setUsers(r.data.data||[])).catch(()=>setUsers([])).finally(()=>setLoading(false))},[]);return <AppShell role="admin" title="User management" subtitle="Manage the people who use the HeartBud platform."><SectionCard title="Users"><table className="hb-table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th></tr></thead><tbody>{loading?<tr><td colSpan="4">Loading…</td></tr>:users.length?users.map(u=><tr key={u.idpatients}><td><strong>{u.fullnames}</strong></td><td>{u.email}</td><td>{u.role}</td><td><StatusBadge tone="healthy">Active</StatusBadge></td></tr>):<tr><td colSpan="4"><div className="hb-empty">No users returned. The V2 admin endpoint may not be running yet.</div></td></tr>}</tbody></table></SectionCard></AppShell>}
export function AdminSimple({title,subtitle,body}){return <AppShell role="admin" title={title} subtitle={subtitle}><SectionCard title={title}>{body}</SectionCard></AppShell>}
