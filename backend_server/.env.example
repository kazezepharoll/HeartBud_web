# Either set DATABASE_URL (a full mysql:// connection string — e.g. Railway's
# ${{MySQL.MYSQL_PRIVATE_URL}}) or the four DB_* variables below. DATABASE_URL
# takes priority if both are set.
DATABASE_URL=
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=CHANGE_ME
DB_NAME=heartbud
JWT_SECRET=CHANGE_ME
SMTP_USER=CHANGE_ME
SMTP_PASS=CHANGE_ME
